export interface ISliceState {
};

export interface IQueryUserInfoResult {
    id: number;
    phone: string;
    nickname: string;
    avatar: string;
    createdAt: string;
    updatedAt: string;
}
